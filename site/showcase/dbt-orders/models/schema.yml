version: 2

sources:
  - name: raw
    tables:
      - name: orders

models:
  - name: stg_orders
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: amount_cents
        tests: [not_null]
  - name: fct_daily_orders
    columns:
      - name: order_day
        tests: [unique, not_null]
