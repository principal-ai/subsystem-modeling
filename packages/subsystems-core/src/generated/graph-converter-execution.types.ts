/**
 * Generated types from canvas: Graph Converter Execution
 *
 * DO NOT EDIT MANUALLY - This file is auto-generated
 * Generated by @principal-ai/subsystems-core
 *
 * Canvas: Execution flow for graph conversion with event schema validation
 * Version: 1.0.0
 */

/**
 * Event types for node: graph-converter
 */
export namespace GraphConverter {
  /**
   * Graph conversion begins
   */
  export interface ConversionStarted {
    name: 'conversion.started';
    attributes: {
      /** Number of node types in configuration */
      'config.nodeTypes': number;
      /** Number of edge types in configuration */
      'config.edgeTypes': number;
    };
  }

  /**
   * Processing node definitions
   */
  export interface ConversionProcessingNodes {
    name: 'conversion.processingNodes';
    attributes: {
      /** Number of nodes being processed */
      'nodes.count': number;
    };
  }

  /**
   * Processing edge definitions
   */
  export interface ConversionProcessingEdges {
    name: 'conversion.processingEdges';
    attributes: {
      /** Number of edges being processed */
      'edges.count': number;
    };
  }

  /**
   * Graph conversion completes successfully
   */
  export interface ConversionComplete {
    name: 'conversion.complete';
    attributes: {
      /** Number of nodes in result */
      'result.nodes.count': number;
      /** Number of edges in result */
      'result.edges.count': number;
      /** Conversion duration in milliseconds */
      'duration.ms'?: number;
    };
  }

  /**
   * Error during conversion
   */
  export interface ConversionError {
    name: 'conversion.error';
    attributes: {
      /** Error message */
      'error.message': string;
      /** Phase where error occurred */
      'error.phase'?: string;
    };
  }

  /**
   * Union of all event types for graph-converter
   */
  export type Event = ConversionStarted | ConversionProcessingNodes | ConversionProcessingEdges | ConversionComplete | ConversionError;

  /**
   * Literal union of event names for graph-converter
   */
  export type EventName = 'conversion.started' | 'conversion.processingNodes' | 'conversion.processingEdges' | 'conversion.complete' | 'conversion.error';
}

/**
 * Event types for node: validation
 */
export namespace Validation {
  /**
   * Validation begins
   */
  export interface ValidationStarted {
    name: 'validation.started';
    attributes: {
      /** Size of configuration object */
      'config.size': number;
    };
  }

  /**
   * Validation completes
   */
  export interface ValidationComplete {
    name: 'validation.complete';
    attributes: {
      /** Whether validation passed */
      'validation.passed': boolean;
      /** Number of validation errors */
      'errors.count': number;
    };
  }

  /**
   * Union of all event types for validation
   */
  export type Event = ValidationStarted | ValidationComplete;

  /**
   * Literal union of event names for validation
   */
  export type EventName = 'validation.started' | 'validation.complete';
}

/**
 * Event types for node: graph-output
 */
export namespace GraphOutput {
  /**
   * Graph output is created
   */
  export interface OutputCreated {
    name: 'output.created';
    attributes: {
      /** Type of output (NodeState[] | EdgeState[]) */
      'output.type': string;
    };
  }

  /**
   * Union of all event types for graph-output
   */
  export type Event = OutputCreated;

  /**
   * Literal union of event names for graph-output
   */
  export type EventName = 'output.created';
}

/**
 * Union of all event names in the canvas
 */
export type AllEventNames = 'conversion.started' | 'conversion.processingNodes' | 'conversion.processingEdges' | 'conversion.complete' | 'conversion.error' | 'validation.started' | 'validation.complete' | 'output.created';

/**
 * Type-safe event emitter for a specific node
 *
 * @example
 * ```typescript
 * const emit: NodeEmitter<GraphConverter.EventName, GraphConverter.Event> = ...
 * emit('conversion.started', {
 *   name: 'conversion.started',
 *   attributes: { ... }
 * });
 * ```
 */
export type NodeEmitter<
  TEventName extends string,
  TEvent extends { name: TEventName; attributes: Record<string, unknown> }
> = (event: TEvent) => void;

/**
 * Type-safe event emitter by event name
 *
 * @example
 * ```typescript
 * const emit: NodeEmitterByName<GraphConverter.Event> = ...
 * emit('conversion.started', {
 *   'config.nodeTypes': 2,
 *   'config.edgeTypes': 1
 * });
 * ```
 */
export type NodeEmitterByName<
  TEvent extends { name: string; attributes: Record<string, unknown> }
> = <TName extends TEvent['name']>(
  eventName: TName,
  attributes: Extract<TEvent, { name: TName }>['attributes']
) => void;